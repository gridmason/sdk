# sdk

`@gridmason/sdk` — the host-SDK interface widgets code against (capability-scoped data access, typed event bus, widget helpers) + the widget-side helper library. Public OSS (AGPL-3.0). Engineering spec: `docs/SPEC.md` · Build plan: `docs/specs/sdk-v0/spec.md`.
