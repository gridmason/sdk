/**
 * The fixture reference implementation (docs/SPEC.md §5): `createFixtureSDK()` —
 * the no-op impl backed by an author-supplied fixture map, so a widget under
 * development receives realistic data instead of empty defaults. Capability
 * checks still run against the manifest (fixture-green predicts review-green).
 * Consumed by `gridmason dev` and data-bearing widget tests. Published at
 * `@gridmason/sdk/fixture`.
 *
 * Placeholder — `createFixtureSDK()` + the fixture file schema land in S-E1
 * (issue #7).
 */
export const FIXTURE_PLACEHOLDER = true;
