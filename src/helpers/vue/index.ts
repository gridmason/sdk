/**
 * Vue widget-side helpers (docs/SPEC.md §4): composables over the shared helper
 * core, mirroring the React reference adapter. Published at
 * `@gridmason/sdk/vue`.
 *
 * Placeholder — the Vue adapter is a Phase-B deliverable (issue #10).
 */
export const VUE_HELPERS_PLACEHOLDER = true;
