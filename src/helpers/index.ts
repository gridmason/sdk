/**
 * Framework-agnostic widget-side helpers (docs/SPEC.md §4): thin ergonomics over
 * the `HostSDK` handle — no privileged logic — so a widget can be audited by
 * reading its SDK calls. The per-framework adapters (`./react`, `./vue`,
 * `./vanilla`) share this core; the React set is the reference (dashboard is
 * React, GW-D16).
 *
 * Placeholder — the helper core lands in S-E1/S-E2 (issues #8, #10).
 */
export const HELPERS_PLACEHOLDER = true;
