/**
 * React widget-side helpers (docs/SPEC.md §4): the reference adapter over the
 * framework-agnostic helper core — `useRecord`, `useSettings`, `emit`/`on`,
 * `scopedFetch` as hooks. Published at `@gridmason/sdk/react`.
 *
 * Placeholder — the React subset is the Phase-A helper deliverable (issue #8).
 */
export const REACT_HELPERS_PLACEHOLDER = true;
