/**
 * Vanilla widget-side helpers (docs/SPEC.md §4): plain functions over the shared
 * helper core, for widgets built without a framework. Published at
 * `@gridmason/sdk/vanilla`.
 *
 * Placeholder — the vanilla adapter is a Phase-B deliverable (issue #10).
 */
export const VANILLA_HELPERS_PLACEHOLDER = true;
